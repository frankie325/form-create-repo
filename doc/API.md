# API

## append
```js
/**
* @description: 新增rule插入到指定位置
* @param {Object} rule 新增的规则项
* @param {String} after 插入到指定表单字段的组件之后
* @param {Boolean} child 是否插入到子节点中
* @return {*}
*/
fApi.append(rule, after, child)
```